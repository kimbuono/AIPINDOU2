# FastAPI 配置
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# JWT 密钥（生产环境请更换为随机字符串）
JWT_SECRET=change-me-in-production

# Token 有效期（小时）
TOKEN_EXPIRE_HOURS=168

# 数据库路径（默认自动创建）
DB_PATH=./data/aipindou.db
